
<%= render 'menu' %>

<script>
	$( document ).ready(function() {

		//var ok;

		//var $mainCatCheckbox = $('<%=   %>').find('input[type="checkbox"]');

		//var $curCatItems = $('.cat-0.items'),
		var	$mainCatCheckboxes = $('.main-cat').find('input[type="checkbox"]'),
			$allItems = $('.items');

		$allItems.hide();

		$mainCatCheckboxes.change(function () {

			var curCatItems = $(this).parents('div').data('children');

			if ($(this).prop('checked')) {
				$(curCatItems).show(); 
			} else {
				var $itemCheckboxes = $(curCatItems).find('input[type="checkbox"]:checked');
				$itemCheckboxes.removeAttr('checked');
				$(curCatItems).hide();
			}
		});

		  
			// $("#blake").click(function(event) {
			// 	// catagories
			// 	if ($('.catbox:checked'))
			// 	{
			// 		ok = $(event.target).parent().parent().parent().attr('id')
			// 		$('[class="'+ok+ ' items hidden"]').removeClass('hidden')
			// 	};

			// 	if ($('.catbox'))
			// 	{
			// 		$('.catbox').each(function(){		
			// 			if (!this.checked){
			// 				if (!$(this).parent().parent().parent().hasClass('hidden')){			
			// 					x = $(this).parent().parent().parent().attr('id')
			// 					$('[class="'+x+ ' items"]').addClass('hidden')
								
			// 				}
			// 			}
			// 		})		
			// 	}

			// 	// items
			// 	if ($('.itembox:checked')){
			// 		itemid = $(event.target).parent().attr('id')
			// 		$('[class="'+itemid+' hidden"]').removeClass('hidden')


			// 	}

			// 	if ($('.itembox')){
					
			// 		$('.itembox').each(function(){
						
			// 			if (!this.checked){
			// 				if (!$(this).parent().hasClass('hidden')){
			// 					y = $(this).parent().attr('id')
			// 					$('[class="'+y+'"]').addClass('hidden')
			// 				}
							
							
			// 			}
			// 		})
	
			// 	}





	

			//   })
			
		

});
</script>